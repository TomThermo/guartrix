node_modules/
dist/
dist-release/
dist-download/
build/
.vite/
.DS_Store
*.log

# Local Cursor / GitHub Actions config — keep on disk, never publish on main.
.cursor/
.github/

# Operator-host / docs / packaging tooling — kept on this machine, not for public clones.
scripts/build-out.sh
scripts/esbuild-release.mjs
scripts/lib-stage-release.sh
scripts/package-download-bundle.sh
scripts/package-release.sh
scripts/prod-web-download.mjs
scripts/install-daemon-le-cert.sh
scripts/install-download-le-cert.sh
scripts/install-license-le-cert.sh
scripts/capture-wiki-screenshots.mjs
scripts/ensure-demo-admin.mjs
scripts/generate-openapi-stubs.mjs
scripts/check-openapi-coverage.mjs
scripts/cloud-agent-start.sh
scripts/cloud-invite-smoke.mjs
scripts/scale-smoke.sh
AGENTS.md
CONTRIBUTING.md
e2e/
playwright.config.ts
docker-compose.dev.yml

# Secrets & local env
.env
.env.local
license.env
# Live license config is data/license.env (under data/, already ignored)
*.pem

# Operator-only license issuer — NEVER in this public panel repo.
# Lives as a sibling checkout (e.g. ../guartrix-license-server), not under apps/.
apps/license-server/
apps/license-server/**
**/guartrix-license-server/

# Operator promo docs (also under data/promo-docs/). Never commit if recreated at these paths.
docs/site/
scripts/docs-site.mjs

# Runtime / generated data (never commit)
data/
# Operator promo docs index (HTTPS :8443) also lives under data/promo-docs/ — never GitHub.
# Keep env templates next to live files (data/*.env.example is tracked)
!data/
data/**
!data/*.example
!data/**/*.example
cert/

# Legacy / local DB files
*.db
*.db-journal
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
dev.db.sqlite-backup
coverage/
