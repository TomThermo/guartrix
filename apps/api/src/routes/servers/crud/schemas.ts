/** @deprecated Import from `../../../schemas/servers.js` — kept for existing route imports. */
export {
  SERVER_TYPES,
  createServerClientSchema as createSchema,
  cloneServerSchema as cloneSchema,
} from "../../../schemas/servers.js";
